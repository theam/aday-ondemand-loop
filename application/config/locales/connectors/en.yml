en:
  connectors:
    dataverse:
      actions:
        connector_edit:
          success: "API Key updated: %{name}"
        dataset_create:
          success: "Dataset created: %{id} > %{title}"
        collection_select:
          success: "Collection selected: %{title}"
        dataset_select:
          success: "Dataset selected: %{title}"
      upload_bundles:
        collection_not_found: "Dataverse collection not found: %{url}. Only public and published collections are supported"
        dataset_not_found: "Dataverse dataset not found: %{url}.<br />Only public and published datasets are supported"
        created: "Upload bundle created: %{name}"
      upload_connector_processor:
        description: "File uploaded from OnDemand Loop application"
    zenodo:
      actions:
        connector_edit:
          success: "Access token updated: %{name}"
        upload_bundles:
          record_not_found: "Zenodo record not found: %{url}. Only published records are supported"
          created: "Upload bundle created: %{name}"
